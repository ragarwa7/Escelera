<% if @cars.length <= 0 %>
  <h1>Cars will be added to the system soon</h1>
<% else %>
  <h1>This will house the car listings</h1>
<% end %>
<% if current_user.try(:admin?) or current_user.try(:superadmin?) %>
  <p>
    <%= link_to 'Add Car', new_car_path, class: 'btn btn-primary' %>
  </p>
<% end %>
